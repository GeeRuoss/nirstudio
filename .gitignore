# build
dist/
.astro/

# deps
node_modules/

# macOS
.DS_Store

# config locale Claude Code
.claude/settings.local.json

# secrets et fichiers sensibles
.env
.env.*
*.pem
*.p8
.secrets/

# polices sous licence (Crystal) : jamais dans ce repo.
# Elles vivent dans le repo privé GeeRuoss/nirstudio-assets et sont
# injectées ici au build (CI) ou copiées à la main en local.
src/fonts/

# sources design HD (restent locales, jamais dans le repo)
all/
sources/
